tailwind.config = {
    darkMode: 'class',
    theme: {
        extend: {
            colors: {
                // DaisyUI handles colors, but we can keep custom ones if needed
            }
        }
    }
}
