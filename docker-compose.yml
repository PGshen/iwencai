version: '3.8'

services:
  app:
    build: .
    container_name: data-scraper-pusher
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/app.db
      - DEBUG=false
    restart: unless-stopped
